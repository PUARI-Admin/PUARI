import { AppCategoryPipe } from './app-category.pipe';

describe('AppCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new AppCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
